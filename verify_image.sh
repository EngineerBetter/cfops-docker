#!/bin/bash

set -eux

aws --version
bbl --version
bosh --version
certstrap --version
cf --version
credhub --version
fly --version
gcloud --version
ginkgo version
go version
gometalinter --version
gpg --help
jq --version
om version
parallel --version
python version
ruby --version
shellcheck --version
which stopover --version
which stopover.v2 --version
terraform --version
uaac --version
yaml-patch --help
yaml2json -version
which yml2env
yq --version
